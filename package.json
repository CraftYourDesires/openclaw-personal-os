{
  "name": "openclaw-personal-os",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "python3 -m unittest discover tests && playwright test",
    "test:python": "python3 -m unittest discover tests",
    "test:guide": "playwright test",
    "guide": "python3 -m http.server 4173 -d guide",
    "pdf": "node scripts/render_pdf.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
